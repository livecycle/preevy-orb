description: >
  Sample description

executor: default

parameters:
  role_to_assume:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - install
  - init
  - checkout
  - up
