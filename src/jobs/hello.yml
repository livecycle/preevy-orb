description: >
  Sample description

executor: default

parameters:
  profile_url:
    type: string
    description: "The Preevy profile URL"
steps:
  - install
  - init:
      profile_url: "<< parameters.profile_url >>"
  - checkout
  - up
