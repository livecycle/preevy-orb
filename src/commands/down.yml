description: >
  Sample description
# What will this job do?
executor: default

parameters:
  args:
    type: string
    required: false
    description: "Additional args to provide to the `up` command."
steps:
  - run:
      name: Down
      command: preevy down
